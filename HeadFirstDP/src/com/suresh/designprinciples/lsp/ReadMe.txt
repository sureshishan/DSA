The LSP is one of the SOLID principles:

"Objects of super class should be replaceable with objects of a sup-er class without altering the correctness of the program."


This principle ensures that a subclass can stand in for its superclass without causing unexpected behaviour
